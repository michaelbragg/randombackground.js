{
  "name": "randomBackground.js",
  "version": "0.1.3",
  "private": true,
  "description": "Attach a random background image to an element from a user supplied list on page load.",
  "author": "Michael Bragg <michael@michaelbragg.net>",
  "license": "MIT",
  "scripts": {
    "dev": "grunt",
    "test": "grunt test --verbose",
    "dist": "grunt dist"
  },
  "devDependencies": {
    "grunt": "0.4.x",
    "grunt-contrib-jshint": "0.9.x",
    "grunt-contrib-concat": "0.1.x",
    "grunt-contrib-uglify": "0.4.x"
  }
}
